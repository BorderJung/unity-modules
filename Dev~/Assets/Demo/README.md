# Demo scenes

Each module gets a small scene here that imports the package and proves it runs. A module is not
considered "released" until its demo scene compiles and plays (see [CLAUDE.md](../../CLAUDE.md) §8).

Planned:
- `Core/` — DeterministicRng determinism check, Log behavior.
- `Events/` — a publisher + subscriber wired through a `VoidEventChannelSO` / `IntEventChannelSO` asset.
